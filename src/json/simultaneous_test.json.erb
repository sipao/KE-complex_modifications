//設定例
{
  "title": "test",
  "rules": [
  {
  "description": "simultaneous test",
  "manipulators":
<%=
  each_key(
    source_keys_list: [["j","g"]],
    dest_keys_list: [["n","n"]],
    from_mandatory_modifiers: ["any"],
    conditions: ["type":"input_source_if","input_sources":["language":"ja"]],
    as_json: true
  )
%>
  }
  ]
}
